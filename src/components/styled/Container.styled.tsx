import styled from "styled-components";

export const Container = styled.div({
  borderStyle: "dashed",
  width: "500px",
  marginLeft: "450px",
  marginTop: "80px",
});

export const Heading = styled.h1({
  textAlign: "center",
});
